# Assignment 2 Report

## Часть 1. CTC Decoding

### Задание 1. Реализация жадного декодирования

В классе `Wav2Vec2Decoder` реализован метод `greedy_decode`.

Результаты на датасете `data/librispeech_test_other`:

- WER: `6.77%` (референсное значение: `10.4%`)
- CER: `2.34%` (референсное значение: `3.5%`)

### Задание 2. Реализация beam search декодирования

В классе `Wav2Vec2Decoder` реализован метод `beam_search_decode`.

Результаты на датасете `data/librispeech_test_other` при `beam_width = 3`:

- WER: `6.70%` (референсное значение: `9.9%`)
- CER: `2.31%` (референсное значение: `3.4%`)

### Оценка на `data/librispeech_test_other`

Для оценки качества декодирования использовались метрики `WER` и `CER`. При `beam_width = 3` метод `beam_search_decode` показал следующие результаты:

- WER: `6.70%`
- CER: `2.31%`

Полученные значения лучше референсных значений из задания (`WER ≈ 9.9%`, `CER ≈ 3.4%`).

### Влияние `beam_width` на качество и время декодирования

Для анализа компромисса между качеством распознавания и вычислительными затратами были проверены значения `beam_width = 1, 3, 10, 50`.

| beam_width | WER, % | CER, % | Время декодирования, сек |
|---|---:|---:|---:|
| 1 | 6.72 | 2.33 | 83.2 |
| 3 | 6.70 | 2.31 | 92.8 |
| 10 | 6.75 | 2.30 | 140.0 |
| 50 | 6.72 | 2.30 | 411.4 |

### График зависимости качества и времени от `beam_width`

Ниже приведён рисунок, показывающий, как изменяются `WER`, `CER` и время декодирования при увеличении `beam_width`.

![Зависимость WER, CER и времени декодирования от beam width](<staff/Screenshot from 2026-03-21 23-44-13.png>)

На рисунке:

- слева показана зависимость `WER` от `beam_width`;
- по центру показана зависимость `CER` от `beam_width`;
- справа показано время декодирования в секундах.

Этот график нужен для демонстрации компромисса между качеством и вычислительными затратами: небольшое увеличение `beam_width` может слегка улучшить метрики, но дальнейший рост ширины луча заметно увеличивает время декодирования.

### Вывод

Реализация жадного алгоритма и `beam search` оказались лучше референсных значений.

При увеличении количества лучей время декодирования растёт линейно, `CER` уменьшается неравномерно в зависимости от количества лучей, а `WER` оказался лучшим при трёх лучах и изменялся нестабильно.

Учитывая время и ошибки, оптимальным является `beam_width = 3`.
